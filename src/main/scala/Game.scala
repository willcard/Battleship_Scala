import board
import user
import artificial


def MainLoop(redBoard: Board, blueBoard: Board) = {

  if isLoosed(redBoard) {
    return "RED"
  }
  else if isLoosed(blueBoard) {
    return "BLUE"
  }



}
